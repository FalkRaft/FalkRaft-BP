#start every function (except falkraftHelp, custom_commands folder and Kits folder):
#anticheat
function anticheat

#optimizations
function optimizations

#load
#tag @a[tag=!loop] add loop
#execute as @a [tag=loop] run function start2

# switch admin's gamemode to creative than default
gamemode c @a[tag=admin,m=default]